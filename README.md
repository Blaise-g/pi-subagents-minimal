# Minimal Subagents for Pi

Planning repository for a deliberately narrow subagent extension for the Pi coding agent.

The intended v1 supports isolated child sessions for one task or a flat parallel batch. Its validated workflows are early research and repository exploration, followed by report-only diff review and simplification review. The implementation contract is being developed through the repository's Wayfinder map before code is written.
