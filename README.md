# Minimal Subagents for Pi

Planning repository for a deliberately narrow subagent extension for the Pi coding agent.

The intended v1 supports isolated child sessions for one task or a flat parallel batch. Its validated workflows are early research and repository exploration, followed by report-only diff review and simplification review. The [canonical v1 implementation contract](docs/spec/v1-implementation-contract.md) is approved and ready for implementation.
