# PROTOTYPE — Workflow behavioral battery

## Question

Which frozen provider-backed workflow trials and deterministic lifecycle scenarios provide credible release evidence for v1 without implying general model compatibility?

## Use

Open `index.html` directly in a browser. Review the proposed claim boundary, workflow corpus, repetition/oracle rules, and lifecycle matrix. The controls demonstrate how the proposed release claim and unread-result recovery state change.

This is a throwaway HITL decision artifact, not the implementation specification or production test suite.

## Quota pilot

[`quota-pilot/FINDINGS.md`](quota-pilot/FINDINGS.md) records a 21-session Luna pilot across low, medium, and high Thinking, projects the complete 63-session matrix, and preserves normalized results plus compressed Pi event streams. Rerun it with:

```bash
bash prototype/workflow-behavioral-battery/quota-pilot/run-pilot.sh
```
